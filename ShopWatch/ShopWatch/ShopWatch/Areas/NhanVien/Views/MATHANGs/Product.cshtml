

@model IPagedList<ShopWatch.Models.MATHANG>
@using PagedList;
@using PagedList.Mvc;
@{
    ViewBag.Title = "Product";
    Layout = "~/Areas/NhanVien/Views/Shared/_Layout1.cshtml";
}
<style>
    .edit-button,
    .delete-button {
        display: inline-block;
        padding: 8px 16px;
        margin-right: 10px;
        background: linear-gradient(to right, #87CEFA, #4682B4);
        border: none;
        color: white;
        font-size: 14px;
        text-align: center;
        text-decoration: none;
        cursor: pointer;
    }

    .bnts {
        background-color: burlywood;
        margin-left: 20px;
    }

    .seache {
        display: flex;
        padding: 20px
    }

    #table-wrapper {
        position: relative;
    }

    #table-scroll {
        height: 700px;
        overflow: auto;
        margin-top: 20px;
    }

    #table-wrapper table {
        width: 100%;
    }

    #table-wrapper {
        position: sticky;
        top: 0;
        z-index: 1;
        background-color: #04AA6D;
    }


        #table-wrapper table thead th .text {
            position: absolute;
            top: -20px;
            z-index: 2;
            height: 20px;
            width: 35%;
            border: 1px solid red;
        }
</style>
@*<div class="row">
        <div class="col-xs-12">
            @using (Html.BeginForm("Product", "MATHANGs", FormMethod.Post))
            {
                @Html.AntiForgeryToken()
                <div class="input-group-container">
                    <div class="seache">
                        <input id="myInput" onkeyup="searchTableColumns()" type="text" name="searchValue" class="form-control" placeholder="Nhập vào giá hoặc tên sản phẩm" aria-label="Search" aria-describedby="basic-addon2">
                        <button class="btn btn-outline-secondary bnts" type="submit">Search</button>
                    </div>
                </div>
            }
        </div>
    </div>*@
<div style="width: 100%; height: 50px; background-color: #f2f2f2; display: flex; align-items: center; justify-content: space-between;">
    <div style="display: flex; justify-content: space-between; width: 100%;">
        <div style="display: flex; justify-content: space-between; width: 100%;">
            <input id="myInput" onkeyup="searchTableColumns()" type="text" name="searchValue" placeholder="Nhập vào giá hoặc tên sản phẩm" aria-label="Search" aria-describedby="basic-addon2" style="width:350px;height:40px;border-radius:10px;border-width:1px">

            <select name="brand" id="brandSelect" style="height: 40px; border-radius: 10px; border-width: 1px" onchange="updateHiddenInputs(); ">
                <option disabled selected hidden>Chọn hãng</option>
                <option value="Heuer">Heuer</option>
                <option value="A.Lange & Söhne">A.Lange & Söhne</option>
                <option value="Jaeger-LeCoultre">Jaeger-LeCoultre</option>
                <option value="Rolex">Rolex</option>
                <option value="Hublot">Hublot</option>
            </select>
            <select name="price" id="priceSelect" style="height: 40px; border-radius: 10px; border-width: 1px" onchange="updateHiddenInputs(); ">
                <option disabled selected hidden>Chọn giá</option>
          
                <option value="1000000">1.000.000</option>
                <option value="2000000">2.000.000</option>
                <option value="3000000">3.000.000</option>
                <option value="4000000">4.000.000</option>
                <option value="7000000">7.000.000</option>
                <option value="10000000">10.000.000</option>
                <option value="20000000">20.000.000</option>
                <option value="30000000">30.000.000</option>
            </select>
            <select name="size" id="sizeSelect" style="height: 40px; border-radius: 10px; border-width: 1px" onchange="updateHiddenInputs(); ">
                <option disabled selected hidden>Chọn size</option>
                <option value="38">38</option>
                <option value="44">44</option>
            </select>
            <select name="type" id="typeSelect" style="height: 40px; border-radius: 10px; border-width: 1px" onchange="updateHiddenInputs(); ">
                <option disabled selected hidden>Chọn loại</option>
                <option value="Đồng hồ cơ">Đồng hồ cơ</option>
                <option value="ĐH ĐIỆN TỬ">ĐH ĐIỆN TỬ</option>
                <option value="ĐH SMARTWATCH">ĐH SMARTWATCH</option>
                <option value="cao cấp">cao cấp</option>
            </select>
        </div>
        <form id="searchForms" action="/NhanVien/MATHANGs/Product" method="post">
            <input type="hidden" id="selectedType" name="selectedType" />
            <input type="hidden" id="selectedBrand" name="selectedBrand" />
            <input type="hidden" id="selectedPrice" name="selectedPrice" />
            <input type="hidden" id="selectedSize" name="selectedSize" />
        </form>
    </div>
</div>


<div class="row">
    <div class="col-xs-12">
        <div class="box ">

            <div class="box-body" id="table-scroll">
                <table id="myTable" class="table">
                    <thead id="table-wrapper">
                        <tr style="background-color:#04AA6D; color: white;" class="header">
                            <th style=" padding: 15px; text-align: center; color:antiquewhite">
                                <label>TENHANG</label>
                            </th>
                            <th style=" padding: 15px; text-align: center; color:antiquewhite">
                                <label>ANHSANPHAM</label>

                            </th>
                            <th style=" padding: 15px; text-align: center; color:antiquewhite">
                                <label>SANXUAT</label>

                            </th>
                            <th style=" padding: 15px; text-align: center; color:antiquewhite">
                                <label> LOAI</label>
                            </th>
                            <th style=" padding: 15px; text-align: center; color:antiquewhite">
                                <label>HANGSANXUAT</label>

                            </th>
                            <th style=" padding: 15px; text-align: center; color:antiquewhite">
                                <label>GIAHANG</label>

                            </th>
                            @* <th style=" padding: 15px; text-align: center;">
                                    <label>BAOHANH</label>

                                </th>*@
                            <th style=" padding: 15px; text-align: center; color:antiquewhite">
                                <label>SALE %</label>

                            </th>
                            <th></th>

                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td class="text-center" style="font-weight:700">
                                    @Html.DisplayFor(modelItem => item.TENHANG)
                                </td>
                                <td class="text-center">
                                    <img src="@item.ANHSANPHAM" alt="Hình ảnh sản phẩm" class="product-image" style="width:40%;height:40%;" />
                                    @* @if (item.ANHS != null && item.ANHS.Any())
                                        {
                                            foreach (var imagePath in item.ANHS)
                                            {
                                                <img src="@imagePath" alt="Hình ảnh sản phẩm" class="product-image" style="width:50%;height:50%;" />
                                            }
                                        }*@
                                </td>
                                <td class="text-center">
                                    @String.Format("{0:dd/MM/yyyy}", item.NGAYSANXUAT)
                                </td>
                                <td class="text-center">
                                    @Html.DisplayFor(modelItem => item.LOAI)

                                </td>
                                @* <td class="text-center ">
                                        @Html.DisplayFor(modelItem => item.KICHTHUOC) MM
                                    </td>*@
                                <td class="text-center">
                                    @Html.DisplayFor(modelItem => item.TENHANGSANXUAT)

                                </td>
                                <td class="text-center" style="font-size:16px;color:brown;font-weight:500">
                                    @String.Format("{0:N0}", item.GIAHANG)
                                </td>
                                <td class="text-center">
                                    @if (item.SALE?.PHANTRAMSALE != null)
                                    {
                                        <text>@Html.DisplayFor(modelItem => item.SALE.PHANTRAMSALE)%</text>
                                    }
                                </td>
                                <td class="text-center">
                                    <div class="btn-group" role="group" aria-label="Button group with nested dropdown">
                                        <div class="btn-group" role="group">
                                            <button id="btnGroupDrop1"
                                                    type="button" style="color:brown"
                                                    class="btn btn-outline-secondary dropdown-toggle"
                                                    data-bs-toggle="dropdown"
                                                    aria-haspopup="true"
                                                    aria-expanded="false">
                                                Tùy chọn
                                            </button>
                                            <div class="dropdown-menu" style="width:200px">
                                                <a href="@Url.Action("EditProduct", new { id = item.MAMATHANG })" class="btn btn-primary">
                                                    <i class="fas fa-edit"></i> Edit
                                                </a>

                                                <a href="@Url.Action("Delete", new { id = item.MAMATHANG })" class="btn btn-primary">
                                                    <i class="fas fa-trash-alt"></i>
                                                </a>

                                                <a href="@Url.Action("Details", new { id = item.MAMATHANG })" class="btn btn-primary">
                                                    <i class="fas fa-info-circle"></i>
                                                </a>
                                            </div>
                                        </div>
                                    </div>
                                </td>
                            </tr>

                        }
                    </tbody>
                </table>

            </div><!-- /.box-body -->
        </div><!-- /.box -->
    </div><!-- /.col -->
</div><!-- /.row -->
<script>
    function searchTableColumns() {
        // Declare variables
        var input, filter, table, tr, i, j, column_length, count_td ;
        column_length = document.getElementById('myTable').rows[0].cells.length;
        input = document.getElementById("myInput");
        filter = input.value.toUpperCase();
        table = document.getElementById("myTable");
        tr = table.getElementsByTagName("tr");
        for (i = 0; i < tr.length; i++) {
            // Check if the row is the header row
            if (i === 0) {
                continue; // Skip the header row
            }
            count_td = 0;
            for (j = 0; j < column_length - 2; j++) { // except first column
                td = tr[i].getElementsByTagName("td")[j];
                /* ADD columns here that you want you to filter to be used on */
                if (td) {
                    if (td.innerHTML.toUpperCase().indexOf(filter) > -1) {
                        count_td++;
                    }
                }
            }

            // Check if it's the header row or if any column contains the filter text
            if (count_td > 0) {
                tr[i].style.display = "";
            } else {
                tr[i].style.display = "none";
            }
        }
    }

    document.getElementById("typeSelect").onchange = function () {
        document.getElementById("selectedType").value = this.value;
    };

    document.getElementById("brandSelect").onchange = function () {
        document.getElementById("selectedBrand").value = this.value;
    };

    document.getElementById("priceSelect").onchange = function () {
        document.getElementById("selectedPrice").value = this.value;
    };

    document.getElementById("sizeSelect").onchange = function () {
        document.getElementById("selectedSize").value = this.value;
    };
    var typeSelect = document.getElementById('typeSelect');
    var brandSelect = document.getElementById('brandSelect');
    var priceSelect = document.getElementById('priceSelect');
    var sizeSelect = document.getElementById('sizeSelect');

    // Gắn sự kiện onchange cho các phần tử select
    typeSelect.addEventListener('change', function () {
        document.getElementById('selectedType').value = this.value;
        document.getElementById('searchForms').submit();
    });

    brandSelect.addEventListener('change', function () {
        document.getElementById('selectedBrand').value = this.value;
        document.getElementById('searchForms').submit();
    });

    priceSelect.addEventListener('change', function () {
        document.getElementById('selectedPrice').value = this.value;
        document.getElementById('searchForms').submit();
    });

    sizeSelect.addEventListener('change', function () {
        document.getElementById('selectedSize').value = this.value;
        document.getElementById('searchForms').submit();
    });
</script>
<div>
    @Html.PagedListPager(Model, page => Url.Action("Product", new { page }))
</div>
@*<script src="~/assets/Admin/plugins/datatables/jquery.dataTables.min.js"></script>
    <script src="~/assets/Admin/plugins/datatables/dataTables.bootstrap.min.js"></script>
    <script src="~/assets/Admin/plugins/datatables/dataTables.alphabetSearch.js"></script>*@